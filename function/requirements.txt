# Core Dependencies
boto3==1.43.3            # AWS SDK for Python to interact with AWS services
botocore==1.43.3         # Low-level, data-driven core of boto3
requests==2.33.1         # To make HTTP requests to EntraID
