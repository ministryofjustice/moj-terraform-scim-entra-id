# Core Dependencies
boto3==1.42.71            # AWS SDK for Python to interact with AWS services
botocore==1.42.71         # Low-level, data-driven core of boto3
requests==2.33.0         # To make HTTP requests to EntraID
