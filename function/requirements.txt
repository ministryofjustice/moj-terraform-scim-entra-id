# Core Dependencies
boto3==1.37.19           # AWS SDK for Python to interact with AWS services
botocore==1.35.92        # Low-level, data-driven core of boto3
requests==2.32.3         # To make HTTP requests to EntraID
